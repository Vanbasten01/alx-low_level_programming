#include <stdio.h>
/**
 *main - Entry point
 *Description: "prints all single digit numbers of base 10 starting from 0"
 *Return: Always 0
 */
int main(void)
{
	int a;

	for (a = 48; a <= 57; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}

